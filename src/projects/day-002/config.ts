import { ProjectConfig } from "../../lib/types";

const config: ProjectConfig = {
  title: "Day 02 - Underwater",
  description: "Text floating on waves",
  categories: [],
  slug: "day-002",
  isPlaying: true,
};

export default config;
