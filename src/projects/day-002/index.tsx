const Day002 = () => {
  return (
    <canvas width={500} height={500} className="w-full h-full bg-blue-500">
      Your browser does not support the canvas element.
    </canvas>
  );
};

export default Day002;
