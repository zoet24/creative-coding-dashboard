import { ProjectConfig } from "../../lib/types";

const config: ProjectConfig = {
  title: "Project",
  description: "",
  categories: [],
  slug: "project-###",
  isPlaying: true,
};

export default config;
