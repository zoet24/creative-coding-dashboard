import { ProjectConfig } from "../../lib/types";

const config: ProjectConfig = {
  title: "Tractor",
  description: "A man and his tractor",
  categories: ["Tractor"],
  slug: "project-003",
  isPlaying: true,
  isAnimated: true,
  isPublished: false,
  controls: [],
};

export default config;
